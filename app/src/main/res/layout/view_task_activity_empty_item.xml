<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

    <include layout="@layout/view_task_activity_spans_item_date"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/task_activity_span_item_padding"
        android:layout_marginRight="@dimen/task_activity_date_text_margin"
        android:gravity="center"
        android:id="@+id/taskActivityEmpty"
        android:text="@string/no_activity"
        android:textSize="@dimen/empty_indicator_text_size"
        android:textColor="@color/empty_indicator"
        android:textStyle="italic"
        android:layout_toEndOf="@+id/textLayout"
        android:layout_toRightOf="@+id/textLayout"
        android:layout_alignTop="@+id/textLayout"
        android:layout_alignBottom="@+id/textLayout"/>
</RelativeLayout>